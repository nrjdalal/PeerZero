NODE_ENV=local

# `bun run dev` serves named portless `.localhost` URLs (branch-prefixed in a worktree); `PORTLESS=0` uses the plain localhost ports below.

# -------------------- Server variables --------------------

HONO_APP_URL=http://localhost:4000
HONO_TRUSTED_ORIGINS=http://localhost:3000
HONO_RATE_LIMIT=60 # allow 60 req/min (default)
HONO_RATE_LIMIT_WINDOW_MS=60000 # 1 minute (default)

# -------------------- Torrent engine --------------------

# The Hono API proxies to the WebTorrent download engine (api/torrent-engine), a
# Node sidecar started automatically by `bun run dev`. Defaults work out of the box.
TORRENT_ENGINE_URL=http://127.0.0.1:4444
# Read by the sidecar itself (plain Node). Port must match TORRENT_ENGINE_URL.
TORRENT_ENGINE_PORT=4444
# Where downloaded files land. Defaults to ~/Downloads/PeerZero (changeable in the app's Settings).
# TORRENT_DOWNLOAD_DIR=/absolute/path/to/downloads
# Per-torrent connection cap. Kept low so a swarm can't exhaust a home router's NAT table.
# TORRENT_MAX_CONNS=25
# Encoded registry the app refreshes from in the background when the upstream lists are
# blocked locally. Defaults to the canary mirror; set to any non-URL value (e.g. off) to
# disable runtime sync.
# REGISTRY_SYNC_URL=

# -------------------- Client variables --------------------

NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:4000
